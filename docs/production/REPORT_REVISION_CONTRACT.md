# Report revision and official-result contract

Updated: September 17, 2026. Owners: Gilfoyle technical integrity, Passan baseball data, Gibs QA. Scope: W03 implementation contract for TI-04/05/09/10, with TI-08 containment and W04 recovery dependencies. This is a design and source inventory, not evidence that every invariant is implemented.

## Current mutation inventory

| Path | Existing behavior and boundary | Required follow-through |
| --- | --- | --- |
| POST game report | Participating coach or commissioner; validates supplied/current-roster data. Creation now uses the shared game lock and commits report, first revision, audit and corrections together. CPU acceptance remains a separate guarded transaction; failure leaves the submitted report pending. | Define identity-bearing submission retries and stable transactional authority/roster/rules. Keep the pending state recoverable between creation and automatic CPU acceptance. |
| PATCH game report | Commissioner full-report edit requires a base version and game then report locks. Body, version increment, edit audit and supplied OCR correction rows now commit together. | The counter identifies retained state snapshots. Supply immutable evidence/rules/roster identities and support authorized amendments without losing prior snapshots. |
| POST report/confirm | Requires the displayed review version. Under game/report locks, checks requested and prepared versions/status plus prepared game fields. Acceptance status/version/audit and official result effects commit together. A matching authorized receipt replays successfully; different or unlinked completed results conflict. | Client must name the revision actually reviewed. Under one transaction recheck authority, state and version; accept exactly that immutable content; write approval, receipt and required projections atomically. An existing receipt must match the same accepted result, otherwise return conflict. |
| POST report/dispute | Requires the displayed review version and serializes against edit/acceptance. Disputed state, proposed score, one version increment and required audit share one transaction. Secondary feed delivery remains best effort. | Bind dispute to reviewed revision. Serialize against edit/confirm and preserve proposals as proposals; do not silently attach a stale dispute to a later draft. Required dispute record and audit commit together. |
| POST report/finalize | Requires the displayed review version. Commissioner selection of reported or corrected scores remains fully validated. Status/version, audit with original/resulting scores and resolution source, and official result effects share the acceptance transaction. | Accept a named complete revision with explicit resolution reason. A score proposal cannot bypass box/innings reconciliation. Preserve original and corrected revisions; atomic receipt plus approval plus projections. |
| PATCH game score | Direct commissioner score entry rejects active reports under the shared game lock and calls atomic finalizer with no box. | Use common game eligibility lock and authoritative result identity. Never allow report creation or approval to produce a second contradictory official record. |
| finalizeReportedGame / finalizeGameAtomic | Reported acceptance checks both client review version and prepared report snapshot under game then report locks. Report status, decision audit and existing core result projections share the transaction. Reported acceptance receipts now identify the accepted snapshot, actor, request version/action/resolution and support exact authorized replay. Legacy and simulated receipts have no report revision reference. | Promote sentinel into a durable accepted-result receipt; validate that caller's named revision remains current under the lock. Return identity-bearing idempotent success or conflict, not silent acceptance of a different snapshot. |
| Simulation and advance | Production callers use atomic finalization with durable coach contributions, even when supplying a legacy accumulator. Participating finalizers serialize projection writes per league. Regular/exhibition/conference finalizer failures now propagate; exhibition completion is transactional. | Postseason milestones now have durable team/season receipts; extend simulated-result identity, legacy reconciliation and coordination with external writers. A bounded full-season SR advance now passes real checkpoint/award fault and fresh-process retry; changed-source/phase-flip uncertainty now refuses continuation and exact-source handoff inherits checkpoints; automatic reconciliation and overlapping lease-loss recovery remain open. Serialization does not establish game chronology. |
| Evidence upload / re-extract / delete and OCR corrections | Evidence/OCR state remains mutable by game. PATCH corrections now commit with report edit/version/audit; initial submission corrections now share the first revision transaction too. | Accepted revision owns an immutable evidence manifest and extraction versions. Late OCR completion or deletion cannot replace an accepted source. Store amendments with provenance. |

Source inventory: [report routes](../../server/routes/games.ts), [game finalizer](../../server/game-finalizer.ts), [simulation routes](../../server/routes/simulation.ts), [schema](../../shared/schema.ts). Correction normalization is in [report-corrections](../../server/lib/report-corrections.ts); transition locking is in [report-transition](../../server/lib/report-transition.ts). W03 batch 03 still does not recheck role or roster revisions inside the transaction.

## Required accepted-result invariants

1. **Revision identity.** A report has stable league/game/report identity. Every content revision is append-only and identifies its predecessor, normalized observed data, completeness, author, reason, creation time, rules/roster contract versions and evidence manifest. An edit counter is a concurrency token, not a substitute for retained historical content.
2. **Reviewed identity.** A confirm, dispute or commissioner resolution names the exact revision the actor saw. Refetching must not silently replace that identity underneath a retained draft or review. The server checks current authority and legal state again inside its transaction. Unknown or stale versions fail without writes; the client preserves work and asks the actor to compare the newer content.
3. **One official receipt.** Each game has one active accepted-result identity. Its receipt references the accepted immutable revision (or seeded simulation result), approving actor and authority, finalization algorithm/version, time and durable effect identity. A retry for the same operation returns that same receipt; a different requested result conflicts. Historical receipts and supersession remain inspectable.
4. **Atomic required effects.** Approval/state, receipt, official game, standings, player lines, pitcher workload/rest, coach record/XP and required audit either commit together or remain unchanged. Durable staged effects require an explicit resumable state that cannot masquerade as complete. No correctness-critical effect can exist only in an in-memory batch accumulator.
5. **Defined ordering.** All competing writers take the game lock before the report lock. They re-read state under those locks. Report creation, approval, dispute, direct-score finalization, simulations and correction use one compatible protocol. Multi-game projection locking must also have a deterministic team/player order to avoid cross-game deadlocks; game-level locking alone does not establish league chronology.
6. **Baseball reconciliation.** Corrections validate complete normalized content before acceptance. Runs, inning lines, team totals, actual roster identities and player counters agree under the declared supported rules. Unknown statistics remain unknown, with no fabricated player lines. Rule-specific inning/tie/mercy/workload choices remain in the [pending inputs](POWER_PROS_CONTRACT_INPUTS.md), not inferred from current restrictions.
7. **Traceable amendments.** Correcting an accepted result creates an authorized superseding revision and reason. Either reverse/reapply explicit per-game contributions or rebuild dependent projections deterministically; do not add the new totals on top of the old. Preserve original evidence, approvals and receipts. Rest chronology and downstream games must be reconciled or explicitly block the amendment.
8. **Evidence and recovery.** Accepted manifests retain source hashes/identifiers, extraction version and correction identity. Pending OCR jobs cannot mutate accepted evidence. A consistent backup restores revisions, receipts, required effects and evidence relationships as one state. Missing legacy evidence remains explicitly unknown rather than fabricated.
9. **External effects.** Inbox/news/recap work is distinct from official acceptance. If reliable delivery is promised, persist an outbox event keyed by receipt within the acceptance transaction and deliver idempotently after commit. Best-effort notifications are not proof of durable delivery or player receipt.

## W03 batch 01 bounded acceptance

The first slice must reject absent, malformed and stale PATCH versions; allow one of two simultaneous edits at the same base version; increment once; reject edits after a completed game or finalization sentinel even if report status is stale; and commit the edit plus required edit audit together. Existing populated reports must gain a valid initial version without changing their baseball data. The client must capture the version with the loaded edit draft and retain it through background refetches and failed submissions.

Independent synthetic HTTP/PostgreSQL checks must inspect persisted report and audit rows, use real authenticated roles and inject an audit failure to prove rollback. A pure draft/version check is useful but cannot certify browser navigation, reload recovery or visual usability. This slice keeps TI-05 implementing until confirm/dispute/finalize and immutable acceptance satisfy the broader contract.

## W03 batch 02 bounded acceptance

[W03 batch 02](W03_BATCH_02.md) implements reviewed-state guards for confirmation, dispute and commissioner resolution, atomic decision/audit with core finalization, and atomic PATCH correction provenance. The existing field name `editVersion` now advances for disputes and acceptance as well as body edits; it is a current-state concurrency token. Deterministic queued race tests cover both orders of edit/confirm, dispute/confirm, edit/finalize and duplicate confirmations. CPU acceptance failure leaves pending state. Secondary feed failures do not return a failed decision after its transaction committed.

Batch 02 did not yet retain content or an identity-bearing receipt. Batch 03 below supersedes those specific limits; authority, evidence and roster boundaries remain open.

## W03 batch 03 retained history and report receipts

[W03 batch 03](W03_BATCH_03.md) appends database-protected snapshots at submission, edit, dispute and acceptance. New reported receipts reference the actual accepted snapshot and decision identity; authorized exact retries return the same receipt without effects. Initial submission shares the common game lock and an atomic provenance transaction. Legacy baseline history observes current data only, without inventing earlier states or accepted identities. Explicit parent deletion retains normal owned-league cleanup semantics.

The existing restore implementation omits the new ledger/receipts, so restoring any current or target report-bearing league is blocked until W04 history-safe recovery. This is loss prevention, not a complete snapshot implementation. Receipt immutability is application-enforced; history snapshots have database update/direct-delete guards. Simulation identities, stable authority/rosters/rules, immutable evidence, amendments and durable cross-game effects remain open.

## W03 batch 04 durable coach contributions

[W03 batch 04](W03_BATCH_04.md) adds receipt-linked coach deltas and observed before/after state, with no fabricated legacy backfill. Atomic callers no longer defer coach rewards to a volatile accumulator. Participating finalizers take league KEY SHARE, game/report locks, the league advisory projection lock and sorted coach row locks; required rivalry errors roll back the transaction. Other coach writers are not certified by that protocol. Contribution rows are durable and application append-only, without database immutability triggers.

The restore guard now also refuses completed games and receipt/effect-bearing current or target states. League locking coordinates new report creation/finalization with destructive restore. This is containment until a complete recovery format exists. Required advance errors propagate in the audited call sites, but actual advance/checkpoint failure and restart journeys remain unverified.

## W03 batch 05 postseason contributions

[W03 batch 05](W03_BATCH_05.md) stores team/season milestone identity, original coach attribution, actual XP/skill-point/counter deltas and before/after observations atomically. Same-source retries return the original receipt, including no-coach dispositions. Award and game finalization writers share the league projection lock. Migration 0054 fences ambiguous old postseason seasons without inventing receipts or changing coach totals; reconciliation remains required. Restore refuses current or target award/fence evidence. Actual advance/checkpoint and Power Pros postseason journeys remain uncertified.

## W03 batch 06 postseason and checkpoint integration

[W03 batch 06](W03_BATCH_06.md) repairs venue-reversed SR win counting, validates complete bracket inputs before projection writes, and refuses contradictory terminal history. Reported preflight selects current postseason fixtures regardless of week zero and requires accepted-report receipts; regular/force advance and quicksim rejection use real HTTP evidence. Runtime fault injection exposed unordered progress writes; all checkpoint calls now await persistence and require operation ownership. The actual full-season SR advance passes required-checkpoint failure, partial award failure and fresh-process retry with the same original receipt and four CWS opening games. Phase flip and checkpoint are still separate writes; no general crash/lease recovery certification is claimed.

## W03 batch 07 interrupted-operation preservation

[W03 batch 07](W03_BATCH_07.md) derives recovery-required refusal from unsettled operation source identity, validates checkpoints and copies progress atomically into exact-source successors. A hard-killed process after committed phase flip cannot silently continue the next phase after restart. Failed-status persistence failure retains its lease; clear-stuck preserves progress and restore refuses operation-bearing history. The commissioner status panel exposes the recovery requirement. This is containment plus exact-source resume, not automatic repair or overlapping old/new-worker fencing.

## Next implementation and verification order

1. Fence a live superseded execution from a successor's checkpoint writer and stage mutations, then prove overlapping lease takeover and standard-mode recovery. Batch07 verifies source-state containment, actual post-flip process death and terminal-write faults, but ambiguous changed-state transitions still need durable target/effect evidence and audited reconciliation. Preserve ordered inherited checkpoints and correct team-identity winner counting. Implement legacy-award reconciliation. Extend per-player/standings contributions, coordinate external coach writers and define chronological rest ordering. Extend identity-bearing simulated receipts and retain authority/rules/roster/evidence snapshots. Keep initial-submission retry and CPU two-transaction recovery explicit.
2. Implement history-safe mature-state capture/restore and response-loss recovery of effects. Fault-inject each required boundary and prove report, receipt, game and projections name one result after restart. Remove the temporary report-restore guard only after those tests pass.
3. Implement auditable amendments and reconciliation with immutable evidence manifests. Test mixed score-only/full reports and simulation results against the same result identity contract.
4. Run mature-state recovery and two-coach/commissioner browser journeys, including interrupted edits, stale review, conflict resolution and receipt inspection. Human usability and baseball enjoyment remain separately measured gates.
