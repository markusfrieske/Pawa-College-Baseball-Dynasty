# W02 batch 05 independent score-only and notification audit

Date: 2026-09-15. Review lenses: JD player experience, Passan baseball data integrity and Gibs integration QA. This reviewer owns this document only and does not implement application or test code.

**Verdict: bounded PASS. No milestone-blocking defect remains in the explicit commissioner score-only new-report path or participating-coach pending notifications.** Independent unit, component-browser and final HTTP checks pass. New score-only submissions retain absent statistics as unknown through official finalization. W02-NOTIFY-01 meets its recipient-routing acceptance; full UX-01/UX-03 acceptance remains open.

## Initial critical review

| Severity | Reproduction and impact | Required acceptance |
| --- | --- | --- |
| P1 · data truth | Submit only final runs as commissioner. Existing POST and report columns default omitted hits/errors to zero; a zero is an asserted baseball fact, not an unknown value. | Preserve missing summary statistics and boxes through submitted payload and database persistence; retain deliberate full-report zeroes. Exercise actual PostgreSQL persistence, not only a response mock. |
| P1 · authority | Forge a score-only mode or stale commissioner flag from an involved ordinary coach. | Server-derived roles remain authoritative. An ordinary coach still must provide the required full box. Missing client role metadata fails closed. |
| P2 · draft loss | Enter full batting/pitching/inning data, choose score-only, then return to full entry. | Switching mode retains the full draft; submitting score-only excludes hidden stats and obsolete OCR corrections. Explicitly explain what is omitted. |
| P2 · W02-NOTIFY-01 | Submit on behalf of two human teams as commissioner with no team or an unrelated team. The former routing omits both coaches or chooses only the home coach. | Notify both participating coaches, deduplicate by real user, exclude the submitter from pending notices, retain involved-coach opponent routing and against-CPU auto-confirm behavior. Verify synthetic notification records. |
| P1 · downstream completeness | Finalize a score-only report. Existing `finalizeReportedGame` converts absent hits/errors to zero and constructs empty box sides. | Preserve missingness in the official result or retain a clearly scoped follow-up; do not claim complete truthful display or finalization if only submission is fixed. |

Initial source: [POST and report routes](../../../server/routes/games.ts), [report table schema](../../../shared/schema.ts), [report finalizer](../../../server/game-finalizer.ts) and [prior milestone](../W02_BATCH_04.md). The role distinction is commissioner versus involved coach, not a client-selected exemption. Commissioner PATCH currently requires a full report; this milestone must not present an unsupported score-only edit action.

## Final verification

| Gate | Independent result | Evidence boundary |
| --- | --- | --- |
| Configured unit suite | **126 passed**, exit 0 | Includes score-only builder omission/retention and recipient selection/deduplication. |
| Score-only mode browser | **31 assertions passed**, exit 0 | Actual mode control and shared payload builder, fresh installed headless Edge at 375 pixels, synthetic parent state. |
| HTTP/PostgreSQL | **229 assertions passed**, exit 0 | Real sessions, route authority, stored report/notification rows, populated-schema migration/readiness and actual finalization. |

Reviewer commands: `node node_modules/@playwright/test/cli.js test --config=playwright.unit.config.ts`, `node --import tsx scripts/verify-report-entry-mode.ts`, and `node --import tsx scripts/verify-reported-results.ts`. The HTTP gate creates and drops its own randomly named database using an explicit loopback test connection; the reviewer did not stop the shared parent-owned fixture cluster. The browser blocks external network requests and removes its owned output after closing its browser and server.

Source review confirms the [report page](../../../client/src/pages/report-game.tsx) exposes the selector only to a current commissioner creating a new report. The actual payload path calls the [shared score-only builder](../../../shared/reporting.ts), which copies only runs and an applicable normalized reason, explicitly sends seven unknown summary/box fields as null and excludes retained rows/OCR corrections. A separate score-only score draft prevents mode changes and score edits from rewriting the retained full report's linescore or direct score. Roster/full-box validation is bypassed only for the explicit new-report mode; role, reason and score checks remain active. PATCH retains its full-report requirements. This parent integration is source-reviewed, not independently exercised as a complete page.

The [mode control](../../../client/src/components/report-entry-mode.tsx) uses a labeled native radio group, visible scope/unknown-stat guidance and draft-retention explanation. Browser execution checks exclusive arrow-key selection with retained focus, label clicks, null payload fields, no leaked player/correction data, and restoration of synthetic draft state after returning to full entry.

The [recipient helper](../../../server/lib/report-notification-recipients.ts) derives recipients from actual participating teams and real users, excludes the submitting actor, and deduplicates across repeated records. Real HTTP evidence checks both no-team and unrelated-team commissioners: exactly both participating coaches receive pending messages, with accurate on-behalf language and schedule links, while the submitter receives a separate receipt. Ordinary/involved commissioner coaching reports keep opponent-only pending routing. The route awaits these safe inbox writes; helper failures remain logged best-effort failures, without durable retries or an atomic outbox claim.

The final HTTP execution verifies explicit null reports preserve all seven unknown fields in PostgreSQL, finalize through both confirm and commissioner-finalize routes to actual scores with no invented box score, and add no fabricated player-season lines. Unauthorized involved coaches cannot gain a score-only exemption with client flags; missing commissioner reasons still reject without persisted writes. Original full-report validation/finalization and audit-reason coverage remains present. Confirmation inbox rows reach both coaches and say the result was recorded, without claiming absent player statistics were updated. The page's confirmation copy now refers to participating coaches; that wording received source review.

The nullable migration was assigned the new [0050 key](../../../server/migrations/0050_report_unknown_summaries.sql) during integration after noticing 0040 was already occupied. It only drops NOT NULL on the four summary columns; existing defaults and historical values are preserved because old coverage cannot be inferred. The [readiness marker](../../../server/lib/runMigrations.ts) advances to this migration. The final gate restores the earlier NOT NULL constraints in a populated owned database, removes only the new migration marker, observes failed version readiness, runs the real migration runner, verifies exactly 0050 was applied and recorded as head, then observes successful version readiness. All existing report rows remain unchanged and the four columns become nullable. Fresh bootstrap also completes in the same owned fixture; this is not a new full production-entrypoint rehearsal.

Review caught that an unconditional absent-box shortcut would discard known team hits/errors from legacy summary-only reports. The finalizer now omits the box only when all four summaries are explicitly unknown and both box sides/innings are absent. When team summaries exist without player boxes, it stores only observed runs/hits/errors with empty player arrays; unknown values stay null and absent AB/RBI/other counters are not invented. Final HTTP checks cover both known and mixed-known summaries, their exact persisted values, and unchanged player-season statistics. The earlier 213-assertion run passed before these refinements; the independent 229-assertion rerun supersedes it for final server evidence.

The parent reports full configured TypeScript passed before final integration; the final extended TypeScript result belongs in the milestone evidence. It is separate from this reviewer's executed gates.

## Evidence limits

The actual component with synthetic state is a bounded browser check, not the full authenticated report page. Production CSS/media, reload recovery, complete role/error/retry journeys and human usability remain open. This milestone does not establish text-simulation gameplay quality or balance. Power Pros edition and ending rules remain unresolved. Notification inserts are not proof of delivery to a human or durable exactly-once retry semantics.

Missingness presentation is deliberately bounded to the new explicit-null score-only path. Legacy omission/default behavior and mixed/partial reports are not broadly reinterpreted. Historical zeroes are not backfilled to unknown. The schedule's existing null-box branch says the box score is unavailable; it does not fabricate visible H/E totals.

**P2 · existing UX-07 follow-through:** a mixed summary-only result retains null H/E in storage, but [the schedule modal](../../../client/src/pages/schedule.tsx) still renders those null values through zero fallbacks when a box exists. [Recap completeness](../../../server/lib/recap-generator.ts) also uses box presence as its completeness signal. Reproduce with a commissioner report containing known home hits, unknown away hits and no player data, then finalize and open its box/recap. Acceptance: render unknown observations explicitly and base recap/stat completeness on supplied data, not box presence. Carry this under the existing UX-07/W03–W06 work; do not duplicate the backlog or claim it resolved by the all-unknown score-only path. This is source-established downstream follow-through, not a new browser reproduction.

Against-CPU auto-confirm logic is unchanged by source inspection; the 229-assertion runtime matrix does not independently certify that branch. Report/audit/notification writes still do not constitute an atomic immutable revision system. W03 integrity and durable result history remain separate gates.
