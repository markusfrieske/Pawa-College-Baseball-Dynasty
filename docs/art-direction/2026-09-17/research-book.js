'use strict';
const $=id=>document.getElementById(id), esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let data, current='recruiting';
const studies=[
 ['hub','Matchday brief','One next decision, then the week.','cinema'],
 ['roster','Roster manifest','Compare athletes without repeated panels.','rows'],
 ['roster-depth','Diamond & batting order','Arrange the team in baseball space.','field'],
 ['recruiting','Recruiting board','Needs, targets and investment together.','ledger'],
 ['recruit-filters','Recruiting filters','An editing tool, not a destination dashboard.','filter'],
 ['recruit-profile','Scouting dossier','What we know, what we still need to learn.','profile'],
 ['schedule','Series calendar','See time, rest and matchups.','calendar'],
 ['report','Game scorebook','Enter facts, resolve errors, then submit.','score'],
 ['postseason','Bracket canvas','Follow the path through the tournament.','bracket'],
 ['commissioner','Commissioner queue','Resolve blockers in a clear order.','queue'],
 ['stats','Statistics ladder','Compare a named metric in context.','ledger'],
 ['program','Program history','A season-spanning legacy timeline.','timeline'],
 ['inbox','Coach correspondence','Read, decide and return to your work.','letters'],
 ['title','Season entrance','A place and an invitation to play.','cinema'],
 ['commits','Incoming class','A group of people, not rows of leads.','profile'],
 ['storylines','Story scene','People, tension and meaningful choices.','paper'],
];
const portrait=i=>`<img src="assets/screen-player-${i}.svg" alt="Fictional PAWA athlete">`;
const rows=(head,items)=>`<div class="rows"><div>${head.map(v=>`<span>${esc(v)}</span>`).join('')}</div>${items.map((r,i)=>`<div class="${i===1?'selected':''}">${r.map(v=>`<span>${esc(v)}</span>`).join('')}</div>`).join('')}</div>`;
function drawing(id){
 switch(id){
 case 'hub':return `<div class="two"><div><p class="kicker">FRIDAY / WEEK 04</p><h3>Your next game is waiting.</h3><div class="scoreboard"><b>CV</b><small>vs</small><b>BS</b></div><p>Cedar Valley · Bay State<br>External game not yet reported</p><span class="sample-action">Prepare game →</span></div><div class="plane"><h4>Before first pitch</h4><div class="queue-row"><b>1</b><span>Confirm the lineup</span><em>Needs review</em></div><div class="queue-row"><b>2</b><span>Check pitching availability</span><em>Open staff</em></div><p style="margin-top:20px">Then: Saturday rematch · Sunday series finale</p></div></div>`;
 case 'roster':return `<p class="kicker">TEAM / PERSONNEL</p><h3>Program roster</h3><div class="strips"><b>All players</b><span>Hitters</span><span>Pitchers</span><span>Columns: Overview</span></div>${rows(['PLAYER / POS','YEAR / ROLE','OVR'],[['Noah Chen · C','JR · Starter','394'],['Jalen Brooks · SS','FR · Starter','380'],['Mateo Rivera · CF','SO · Starter','387'],['Eli Price · 1B','SR · Reserve','401'],['Andre Walker · 2B','FR · Reserve','408']])}<div class="strips"><span><b>Selected:</b> Jalen Brooks</span><span>Inspect profile ↗</span><span>Compare selected players ↗</span></div>`;
 case 'roster-depth':return `<div class="two"><div><p class="kicker">DEFENSIVE ASSIGNMENTS</p><div class="pitch">${[['LF',22,24],['CF',50,12],['RF',78,24],['3B',28,54],['SS',41,43],['2B',62,43],['1B',75,55],['SP',50,64],['C',50,88]].map(([p,x,y])=>`<span class="position" style="left:${x}%;top:${y}%">${p} · Select</span>`).join('')}</div></div><div><h3>Batting order</h3><ol class="lineup">${['Jalen Brooks · SS','Mateo Rivera · CF','Noah Chen · C','Eli Price · 1B','Andre Walker · 2B','Kenji Sato · 3B','Owen Lewis · LF','Miles Turner · RF','Select DH'].map(v=>`<li>${v}</li>`).join('')}</ol><p>Select a player, then a slot. Drag is optional.</p></div></div>`;
 case 'recruiting':return `<p class="kicker">YOUR BOARD / THIS TURN</p><h3>Build the next class.</h3><div class="strips"><b>Needs: SP · C</b><span>Contact budget: remaining / cap</span><span>Visits: used / cap</span></div>${rows(['PRIORITY / PROSPECT','KNOWN STATUS','NEXT DECISION'],[['01 · Alex Morgan · SP','Partial scouting','Scout or contact'],['02 · Taylor Brooks · C','Offer eligible','Review cost'],['03 · Casey Rivera · CF','Follow-up due','Keep or deprioritize'],['04 · Drew Reed · RP','Unscouted','Investigate']])}<div class="strips"><span>Favorites</span><span>Needs match</span><span>Actions available</span><b>Filters ↗</b></div><p style="margin-top:16px">A target receives detail space only when you open it. Keep the board visible while prioritizing.</p>`;
 case 'recruit-filters':return `<p class="kicker">OVERLAY / REFINE THE PROSPECT LIST</p><h3>Find the missing piece.</h3><div class="filters"><div class="filter-field">Position<b>Pitchers</b><span class="chip">SP</span><span class="chip">RP</span><span class="chip">CP</span></div><div class="filter-field">Scouting status<b>Any knowledge level</b><span class="chip">Include unknowns</span></div><div class="filter-field">Board status<b>Not committed</b><span class="chip">My targets</span></div></div><div class="strips"><span>Active: Pitchers</span><span>Clear all</span></div><div class="filter-result"><div class="big-num">23 <small>illustrative matches</small></div><span class="sample-action">Apply & return to board</span></div><p>Visits and action history stay separate tools. Applying filters retains the selected prospect and list position.</p>`;
 case 'recruit-profile':return `<p class="kicker">SCOUTING REPORT / PARTIAL KNOWLEDGE</p><h3>Alex Morgan <small>SP · Cedar Falls, IA</small></h3><div class="folio">${portrait(2)}<div><h4>Revealed attributes</h4><div class="grades"><span>Velocity</span><b>Known range</b><span>Control</span><b>Known range</b><span>Stamina</span><b>Unknown</b><span>Development</span><b>Not revealed</b></div></div><div><h4>Coach's assessment</h4><p>Separate observed strengths from uncertain projections. Keep the same attribute order when comparing pitchers.</p><h4>Why pursue him?</h4><p>Current rotation need · Interest context · Affordable next action</p><span class="sample-action">Review scouting cost</span></div></div>`;
 case 'schedule':return `<div class="strips"><b>← Week 04 →</b><span>Series view</span><span>Season overview</span></div><h3>One week. One series at a time.</h3><div class="calendar-grid"><div><small>WEDNESDAY</small><b>Off day</b><p>Prepare roster<br>Review staff</p></div><div class="game-day now"><small>FRIDAY</small><b>CV / BS</b><span class="status">Prepare</span></div><div class="game-day"><small>SATURDAY</small><b>BS / CV</b><p>Not played</p></div><div class="game-day"><small>SUNDAY</small><b>CV / BS</b><p>Series finale</p></div></div><p style="margin-top:16px">Selected game opens its workspace. Official, pending and unplayed remain different states.</p>`;
 case 'report':return `<div class="strips"><b>1 · Result</b><span>2 · Player lines</span><span>3 · Evidence</span><span>4 · Review</span></div><div class="scoreboard"><span>CEDAR VALLEY</span><b>3 : 2</b><span>BAY STATE</span></div><div class="score-lines">${['SCORE-ONLY DRAFT','R','H','E','INN','Cedar Valley','3','Unknown','Unknown','Unknown','Bay State','2','Unknown','Unknown','Unknown'].map(s=>`<span>${s}</span>`).join('')}</div><div class="strips"><b>Draft · Not submitted</b><span>Score-only requires permission</span></div><p style="margin-top:16px">Validate next to the field. Save progress without accepting the result. A review receipt is a separate state.</p><span class="sample-action">Review supplied result →</span>`;
 case 'postseason':return `<p class="kicker">POSTSEASON / ILLUSTRATIVE CONNECTIONS ONLY</p><h3>Follow the road ahead.</h3><div class="bracket-tree"><div><div class="seed">Pairing A<br>Team / Team</div><div class="seed">Pairing B<br>Team / Team</div></div><div><div class="seed">Next round<br>Winner / Winner</div></div><div><div class="seed">Championship<br>Not yet decided</div></div></div><p>The actual league format determines rounds and advancement. Focus one round on compact screens; retain adjacent-round context.</p>`;
 case 'commissioner':return `<p class="kicker">LEAGUE OPERATIONS</p><h3>What prevents the next step?</h3><div class="strips"><b>2 decisions</b><span>1 waiting on a coach</span><span>Audit history ↗</span></div><div class="queue-row"><b>!</b><span><strong>Reported result requires review</strong><br>Open submitted revision and evidence</span><em>Review</em></div><div class="queue-row"><b>!</b><span><strong>Schedule conflict</strong><br>Inspect affected teams and dates</span><em>Inspect</em></div><div class="queue-row"><b>…</b><span><strong>Readiness pending</strong><br>Show who, why and the applicable deadline</span><em>Waiting</em></div><p style="margin-top:18px">Advance is a consequence of resolved gates, not the biggest button on every page.</p>`;
 case 'stats':return `<p class="kicker">STATISTICS / FICTIONAL COMPLETE RECORDS / SAMPLE ≥ 100 PA</p><h3>On-base leaders</h3><div class="strips"><b>Season 02</b><span>Batting</span><span>Qualified</span><span>Metric: OBP · 120 PA each in this sample</span></div>${[['01','Noah Chen','.412',85],['02','Mateo Rivera','.398',72],['03','Jalen Brooks','.376',62],['04','Eli Price','.355',50]].map(r=>`<div class="ladder"><span>${r[0]}</span><strong>${r[1]}</strong><i style="--bar:${r[3]}%"></i><b>${r[2]}</b></div>`).join('')}<p style="margin-top:18px">Every metric needs its definition, qualification and completeness. Missing reported stats are excluded with an explanation, never fabricated as zero.</p>`;
 case 'program':return `<p class="kicker">CEDAR VALLEY / THE PROGRAM</p><h3>Seasons become stories.</h3><div class="strips"><span>Identity</span><b>Legacy</b><span>Record holders</span></div><div class="timeline-track"><div><b>2026</b><h4>The beginning</h4><p>First roster · Founding coach</p></div><div><b>2027</b><h4>A season to remember</h4><p>Verified achievements · Team photograph</p></div><div><b>2028</b><h4>The next chapter</h4><p>Current class · Unfinished story</p></div></div><p style="margin-top:28px">Open a season spread for its roster, record and sourced milestones. No invented trophy counts.</p>`;
 case 'inbox':return `<div class="mail"><div class="mail-list"><h4>COACH INBOX</h4><div>Requires decision · 2</div><div>Scout report<br><small>Today</small></div><div>League update<br><small>Yesterday</small></div></div><article><p class="kicker">SCOUTING STAFF / TODAY</p><h3>Your next rotation decision</h3><p>Start with the information the coach needs to act. Link the message to the relevant player or game and preserve the return path.</p><hr><p>Reply or action appears only when this message supports one. Archive and read status are secondary.</p><span class="sample-action">Open related scouting report</span></article></div>`;
 case 'title':return `<div class="cinema"><div><p class="kicker">PAWA / COLLEGE BASEBALL DYNASTY</p><h3>Build something<br>worth remembering.</h3><p>A miniature home campus. Your latest dynasty waits at the entrance.</p><span class="sample-action">Continue Cedar Valley →</span><p style="margin-top:20px">New dynasty · Join a league · Settings</p></div></div>`;
 case 'commits':return `<p class="kicker">THE INCOMING CLASS / SIGNED & VERBAL</p><h3>Meet your next chapter.</h3><div class="class-grid">${[['Alex Morgan','SP'],['Taylor Brooks','C'],['Casey Rivera','CF'],['Drew Reed','RP']].map((r,i)=>`<div class="athlete">${portrait(i)}<h4>${r[0]}</h4><small>${r[1]} · Fictional prospect · ${i < 2 ? "Signed" : "Verbal"}</small></div>`).join('')}</div><div class="strips"><span>Class needs</span><span>Commitment details</span><span>Arrival ceremony: separate proposal</span></div>`;
 case 'storylines':return `<p class="kicker">CLUBHOUSE / ILLUSTRATIVE STORY STRUCTURE</p><h3>“Coach, where do I fit?”</h3><div class="two"><div class="plane"><p>A player asks for a conversation. Establish who they are, what happened, and what the coach actually knows before presenting choices.</p><h4>What is at stake</h4><p>Role expectations · Team context · Existing promises</p></div><div><h4>Your response</h4><div class="queue-row"><b>A</b><span>Discuss a realistic role<br><small>Show known consequence and commitment</small></span></div><div class="queue-row"><b>B</b><span>Explain the open competition<br><small>Show a different defensible trade-off</small></span></div></div></div><p style="margin-top:16px">Authored choices must map to approved systems; this is a composition example, not a new implemented event.</p>`;
 default:return '';
 }
}
function render(id,focus=false){
 const record=data.screens.find(s=>s.id===id)||data.screens.find(s=>s.id==='recruiting');current=record.id;
 history.replaceState(null,'','#'+encodeURIComponent(current));$('screen-select').value=current;
 $('group').textContent=record.group+' / '+record.layoutFamily.replaceAll('-',' ');$('screen-title').textContent=record.title;$('decision').textContent=record.primaryDecision;
 const illustration=drawing(current);
 $('study').innerHTML=illustration?`<div class="study"><div class="study-bar"><strong>PAWA</strong><span>VARSITY CLUB / ORIGINAL COMPOSITION STUDY</span></div><div class="study-body">${illustration}</div></div><p class="note">Wide-screen composition study. Sample controls are illustrative; compact interaction is specified below and is not implemented in this diagram.</p>`:`<div class="no-study"><strong>Screen-specific brief · ${esc(record.layoutFamily.replaceAll('-',' '))}</strong><p>${esc(record.composition)}</p><p>No finished visual prototype is claimed for this screen. Its dedicated layout should be drawn after the workflow is reviewed.</p></div>`;
 const list=v=>Array.isArray(v)?`<ul>${v.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`:`<p>${esc(v)}</p>`;
 $('screen-brief').innerHTML=`<div class="brief-grid"><article class="full"><h3>Why this screen has a different shape</h3><p>${esc(record.difference)}</p></article><article><h3>Composition</h3><p>${esc(record.composition)}</p></article><article><h3>Interaction</h3><p>${esc(record.interaction)}</p></article><article><h3>Keep in the first view</h3>${list(record.keep)}</article><article><h3>Remove or defer</h3>${list(record.remove)}</article><article><h3>Compact behavior</h3><p>${esc(record.compact)}</p></article><article><h3>Dependencies before implementation</h3>${list(record.unapprovedDependencies)}</article></div>`;
 $('references').innerHTML=record.referenceIds.map(id=>{const s=data.sources.find(x=>x.id===id);return `<article class="reference"><a href="${esc(s.url)}" target="_blank" rel="noopener">${esc(s.title)} ↗</a><p><b>Evidence:</b> ${esc(s.evidenceType)}</p><p><b>Observed in the source:</b> ${esc(s.observed)}</p><p><b>Our proposed adaptation:</b> ${esc(s.adaptation)}</p><p><b>Avoid borrowing:</b> ${esc(s.avoid)}</p></article>`;}).join('');
 document.querySelectorAll('[data-study]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.study===current)));
 if(focus){$('review').scrollIntoView({behavior:'instant'});$('screen-title').focus({preventScroll:true});}
}
function options(){const term=$('search').value.toLowerCase(), records=data.screens.filter(s=>`${s.title} ${s.group} ${s.layoutFamily}`.toLowerCase().includes(term));$('screen-select').innerHTML=records.map(s=>`<option value="${esc(s.id)}">${esc(s.title)}</option>`).join('');if(records.some(s=>s.id===current))$('screen-select').value=current;$('coverage').textContent=`${records.length} of ${data.screens.length} screen briefs · ${data.sources.length} source records · 16 composition studies`;$('screen-select').disabled=!records.length;}
$('gallery').innerHTML=studies.map(([id,title,note,style])=>`<button data-study="${id}" aria-pressed="false"><span class="glyph ${style}" aria-hidden="true"><i></i><i></i><i></i></span><strong>${esc(title)}</strong><small>${esc(note)}</small></button>`).join('');
$('gallery').addEventListener('click',event=>{const b=event.target.closest('[data-study]');if(b&&data){$('search').value='';options();render(b.dataset.study,true);}});
$('screen-select').addEventListener('change',event=>render(event.target.value));$('search').addEventListener('input',()=>{if(data){options();if(!$('screen-select').disabled&&$('screen-select').value!==current)render($('screen-select').value);}});
fetch('research-book-data.json').then(r=>{if(!r.ok)throw Error('Research catalogue could not be loaded.');return r.json();}).then(value=>{data=value;options();render(decodeURIComponent(location.hash.slice(1))||'recruiting');}).catch(error=>$('error').textContent=error.message);
